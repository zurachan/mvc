(function () {
    var app = angular.module('app', ['ngAnimate', 'ui.bootstrap', 'ui.router', 'oc.lazyLoad'])

    //app.controller('main', function ($location, $transitions) {
    //    var vm = this
    //    vm.page = 'Trang chủ'

    //    OnInit()

    //    $transitions.onSuccess({}, function () {
    //        let page = $location.path().split('/')[1]
    //        vm.menu.map(x => {
    //            if (x.path == page) x.active = true
    //            else x.active = false
    //            return x
    //        })
    //    })

    //    function OnInit() {


    //    }
    //})
})()
